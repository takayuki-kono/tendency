---
description: Git commit workflow - always share commands, never auto-execute
alwaysApply: true
---

# Git Workflow

## 依頼受付時の順序（Prompt → Docs → Code → Git）
ユーザーから依頼があったとき：**先に** `docs/prompt.md` に依頼を追記 → 関連 docs を更新 → 実装。コード修正が終わったら git コマンドを提示する（自動実行しない）。

## コミット前の必須チェックリスト
コードを修正したら、gitコマンド提示の**前に必ず**以下を全て実行：

1. 構文チェック（`py_compile` 等）を実行
2. 関連する仕様書（`pipeline_specs.md` 等）を更新

## Gitコマンドの提示ルール
1. **自動実行禁止**: `git add`, `git commit`, `git push` を勝手に実行しない
2. **常にコマンドを共有**: コード修正が完了したら、**毎回必ず**実行可能な git コマンドをコードブロックで提示する（ユーザーが求めなくても提示する）
3. **明示依頼時のみ実行**: 「実行して」「run it」「y」など明確な指示がある場合のみ実行
4. **日本語コミットメッセージ**: commit メッセージは必ず日本語で記述
5. **Author設定必須**: `--author="Claude <claude@anthropic.com>"` を必ず含める

## PowerShell環境の制約
- ユーザーのシェルは **PowerShell (Windows)**
- bash の heredoc (`<<'EOF'`) は使えない
- コミットメッセージは必ず **1行の `-m "..."` 形式** で提示する
- **`;` や `&&` でのコマンド連結は禁止**（PowerShellで正しく動作しない場合がある）
- 複数コマンドは **改行区切り**（1つのコードブロック内で改行）で提示する
